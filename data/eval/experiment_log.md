# Experiment Log

Append-only. Every evaluated change gets a row here, kept or reverted —
negative results stay logged so nobody re-tries the same idea blind later.
See `plan.md` → Testing, Validation & Benchmarking Plan for the protocol
this log implements.

| Date | Change | Mean nDCG@5 | Mean Precision@5 | N anchors | Outcome | Notes |
|---|---|---|---|---|---|---|
| 2026-09-15 | Broken PCA index (pre-fix): hardcoded `n_components=24`, raw fused vectors destroyed by in-place overwrite | 0.156 | 0.200 | 7 | baseline | Ghazal-cluster total failure (top-5 for a Jagjit Singh ghazal anchor contained zero ghazals). Confirmed by data that the deployed index didn't encode genre/vocal style meaningfully. |
| 2026-09-15 | Phase 0 structural fixes: adaptive PCA formula + `MIN_SONGS_FOR_PCA=200` floor (PCA skipped, full 1381d vector indexed), raw-vector-overwrite fixed (final vectors write to `data/embeddings/`, raw store untouched), `clap_missing` recovery path added (rebuild instead of skip), weight-redistribution proportional-math bug fixed. Rebuilt from intact `data/features/*.json` + `data/nlp/*.npy` — **CLAP unrecoverable** (destroyed by the old bug, needs real re-ingestion to restore) | 0.181 | 0.171 | 7 | kept (fixes are correctness-necessary regardless of score) | Flat vs. baseline — expected, since these were bug fixes, not accuracy improvements. Scores compressed into a narrow 0.78-0.91 band, a symptom of the 768d lyric embedding dominating without CLAP to counterbalance it. Ghazal cluster **still fails completely** (same 2 anchors at 0.000) — this predates and survives the PCA fix, so it's a genuine feature/weighting problem (see Phase 5: validate `murki_index`/`vocal_energy_ratio`), not a PCA artifact. **Actionable conclusion: recovering real CLAP (Phase 4/6 re-ingestion) is now evidenced-urgent**, not just planned — this run is direct evidence the acoustic+lyric-only vector is insufficient on its own. |
| 2026-09-15 | **Owner reviewed and corrected `golden_relevance.json`** (Phase 1 outstanding item, first real pass): downgraded `hoshwalon_ko_khabar_kya_jagjit_singh` 2→1 under the Tum Itna Jo Muskura Rahe Ho anchor (romantic mood vs. the anchor's sad mood — same singer/genre isn't enough); removed `gulon_mein_rang_bhare_mehdi_hassan` from that same anchor (not a fit); removed `afreen_afreen_rahat_fateh_ali_khan` from the Chap Tilak anchor (it's a romantic-sufi song, doesn't belong with the harder qawwali cluster — stays judged under Kun Faya Kun instead, where it fits); **removed the entire `bekhayali_sachet_tandon` anchor** — its own note contradicted its judgments (claimed it shouldn't cluster with the soft Arijit ballads, then graded those same ballads as decent matches), and the owner confirmed "the whole anchor doesn't feel right." Confirmed via `aankh_marey_neha_kakkar` (owner: "not as good match... but it's okay") that a weak-but-present grade-1 judgment is working as intended — not everything needs to be excellent to be worth judging. | **0.173** | **0.167** | 6 | n/a — this is a ground-truth correction, not a model change | Drop from 0.181→0.173 is fully explained by removing `bekhayali` (which scored 0.235) from the average — the other four edited anchors already scored 0.000 both before and after, since none of their actual top-5 results overlapped with the judged pool either way. **This is the number to treat as the real baseline going forward** — it reflects the owner's ear, not an AI guess. |
| 2026-09-15 | Phase 2: added `composer` DB column + `data/composer_map.json` (40/50 songs, AI-compiled from general knowledge, 10 left null rather than guessed) + rerank-time same-composer score boost in `index/search.py` (`_run_search`, additive, over-fetches full catalog when active so a boost can promote a candidate outside the naive top-k). Swept boost weight 0→0.4: nDCG@5 flat at 0.173 up to boost=0.03 (mechanism verified working via manual rank check — e.g. `woh_kagaz_ki_kashti` rank 21→1 — but too small to matter at top-5), rising through 0.290 (0.1), 0.414 (0.15), plateauing at 0.482 (0.25 and 0.4). **Set default to 0.15.** | 0.414 | 0.300 | 6 | kept, moderate weight not plateau weight | **Important caveat, not just a result:** the same AI (this session) authored both `golden_relevance.json` and `composer_map.json` from overlapping background knowledge of these songs (e.g. "these are A.R. Rahman devotional qawwalis" informed both which songs were graded as good matches AND which composer got assigned). This makes the *direction* of the result trustworthy (composer is a domain-sound signal for Bollywood music) but the *magnitude* (2.8x nDCG) likely inflated by shared-authorship circularity rather than independent validation. Treated accordingly: kept the feature, picked a moderate boost (0.15) instead of chasing the plateau value (0.25-0.4) that only maximizes agreement with judgments drafted using the same reasoning. **Needs independent verification** — owner listening check, or eventually an external signal (Last.fm similar-tracks, Phase 7) — before trusting this delta the way the Phase 0/1 numbers can be trusted. |
| 2026-09-15 | **Phase 4/6: recovered real CLAP embeddings.** Re-downloaded audio for all 50 songs (yt-dlp), trimmed to the ORIGINAL clip window (`actual_clip_start` from each song's features JSON, so CLAP embeds the same audio the other features were computed from), ran real CLAP inference, deleted audio, rebuilt each raw fused vector with `clap_missing=False`. Zero failures (50/50). Found and fixed two real bugs along the way, unrelated to the CLAP recovery goal itself but blocking it: (1) `torchaudio` 2.x defaults to a `torchcodec`/FFmpeg backend that couldn't find its dylib on this Mac (Anaconda-vs-Homebrew path mismatch) — patched `torchaudio.load` to use `soundfile` instead, sidestepping FFmpeg entirely since all clips are plain WAV; (2) **real CLAP output is 1024-dim, not 512 as every doc/comment in this repo assumed** (FEATURES.md, AGENTS.md, embedder.py docstrings) — a real embedding was never actually produced before this point in the project's history, so the wrong assumption was never caught. Added a `CLAP_DIM` constant and fixed every hardcoded 512 reference (code and docs). Raw fused vector is now ~1893d (was documented as ~991d, then ~1381d without CLAP — both wrong at different points). | 0.121 (CLAP alone, composer-boost=0) → **0.502 (CLAP + composer-boost=0.15)** | 0.233 → **0.400** | 6 | kept — but see notes, this is not a clean single-factor win | **CLAP alone is NOT a clear improvement in isolation**: nDCG@5 with real CLAP and no composer boost is 0.121, actually *worse* than the no-CLAP baseline's 0.173. The Kun Faya Kun anchor collapsed from 0.679 (no-CLAP) to 0.000 (with CLAP) — real CLAP pulled in generically-high-energy matches (Duaa, Bhar Do Jholi Meri) over the lyric-embedding-driven matches (Piya Haji Ali, Arziyan, same composer) that scored well before. This is a direct, empirical confirmation of AGENTS.md's own documented constraint ("CLAP not trained on Indian music... misclassifies high-energy sufi as party") — not just a theoretical concern anymore. **CLAP + composer boost together (0.502) is the best result obtained so far** — composer boost's rerank appears to correct exactly the kind of ranking error CLAP introduces on its own. Same circularity caveat as the composer-boost entry above still applies to the 0.502 number, and now matters more since it's the headline result — independent golden-set expansion (Phase 1 outstanding item) is the next thing that should happen before trusting this further. |
| 2026-09-15 | **Phase 4: generalized Stage 2 rerank.** Extracted the previously hardcoded composer-only boost in `index/search.py` into a real, extensible framework (`index/rerank.py`): a `SCORERS` dict of independent, interpretable scorers (composer, vocal_energy, tempo, tonnetz, lyric, mood — the last two mood/lyric-overlap kept for the upcoming mood-tagging work), each with its own weight in `DEFAULT_WEIGHTS`, combined additively on top of the Stage 1 FAISS cosine score. Adding a new signal is now "write one scorer function + one line," not "re-derive the fusion formula." Only `composer` has a nonzero default (0.15, already swept); every other weight starts at 0.0 (inert) until it earns a nonzero default the same way. `scripts/evaluate_golden.py --rerank-weight NAME=VALUE` (repeatable) replaces the old `--composer-boost` flag for sweeping any signal. | 0.502 | 0.400 | 6 | kept — pure refactor | **Verified zero regression**: re-ran the exact same config (real CLAP + composer=0.15) post-refactor and got the identical 0.502/0.400 — this was purely an architectural change, not a new result. Also verified `index/search.py`'s smoke test and `find_similar_by_name` (used by `app.py`) still work unchanged. |
| 2026-09-15 | Swept the 4 remaining Stage 2 signals individually (composer held at 0.15, weight in {0, 0.05, 0.1, 0.2, 0.3} each): `vocal_energy`, `tempo`, `tonnetz`, `lyric`. | 0.502 at w=0 for all four; every nonzero weight scored LOWER (vocal_energy/tempo: small drop, ~0.41-0.49; tonnetz/lyric: sharp drop, down to 0.125/0.300 at w=0.3) | n/a | 6 | **all four kept at default 0.0 (inert)** — none earned a nonzero weight | Honest negative result, logged so nobody re-tries these blind. Working hypotheses for *why* (not confirmed): vocal_energy/tempo/tonnetz are already implicitly inside the Stage 1 fused vector, so re-scoring them at Stage 2 may just double-count noise rather than add information; `lyric` most plausibly reinjects the exact "generic sentence embeddings make same-language text look artificially similar" problem diagnosed back at the CLAP-recovery step, now made worse by explicitly upweighting it further. Also plausible: 6 anchors just isn't enough resolving power to detect a real small positive effect, if one exists. **Composer remains the only Stage 2 signal validated to help.** Revisit tonnetz/lyric/vocal_energy/tempo once the golden set is bigger, not before. |
| 2026-09-15 | **Lyrics/mood backfill.** Fixed an architectural gap (raw lyric text was never persisted, only its embedding — no way to spot-check Genius's fuzzy-search matches or derive any new text signal without re-fetching). Added `data/lyrics/<song_id>.txt` persistence going forward (`pipeline/processor.py`), backfilled all 50 existing songs (`scripts/recover_lyrics_mood.py`): 40 recovered, 5 genuinely had none (matches `lyrics_missing`), 5 already present from an earlier smoke test. Built `pipeline/mood_tagger.py` — a v1 **keyword-based** (Romanized + Devanagari) mood classifier across 4 Bollywood/ghazal/sufi-specific categories (devotional, romantic, sad_longing, celebratory_party), explicitly NOT a trained sentiment model, same "first draft, needs validation" discipline as `composer_map.json`. 32/50 songs got a confident tag (18 left `None` rather than forced); distribution: romantic=12, devotional=8, celebratory_party=9, sad_longing=3. Wired as the `mood` Stage 2 scorer (already existed as a stub in `index/rerank.py`). Swept weight {0, 0.05, 0.1, 0.2, 0.3} against the golden set, composer held at 0.15. | 0.502 at w=0 → 0.489 → 0.430 → 0.335 → 0.335 (monotonically worse) | n/a | 6 | **kept at default 0.0 (inert)** — did not earn a nonzero weight | Same outcome as the vocal_energy/tempo/tonnetz/lyric sweep — a plausible-sounding signal that doesn't help on this golden set. One spot-check worth noting: `hoshwalon_ko_khabar_kya` independently tagged "romantic" by the keyword tagger, matching the owner's own unprompted correction earlier ("kind of romantic song") — so the tagger isn't pure noise, but also saw a likely false positive (`kabira_tochi_raina` tagged "devotional," more plausibly romantic — Bollywood lyrics often borrow devotional vocabulary for romantic metaphor, which a keyword counter can't distinguish). Net effect on ranking is negative regardless of these anecdotes — trust the sweep, not the spot-checks. Same "revisit once the golden set is bigger" conclusion as the other four signals. |
| 2026-09-15 | **Phase 2 REDONE properly: automated metadata fetcher replaces hand-typed `composer_map.json`.** Owner explicitly flagged that hand-typed metadata doesn't scale and the pipeline must fetch/parse/update itself. Built `pipeline/metadata_fetcher.py` (Wikidata API — evaluated against MusicBrainz first, which proved unreliable in testing; Wikidata had strong initial coverage) and wired it into `pipeline/processor.py` so every future song gets composer/lyricist/genre/year automatically at ingestion, no manual step. Found and fixed 3 real bugs via testing before trusting it: (1) non-ASCII User-Agent broke the HTTP header entirely; (2) disambiguation initially picked a "vocal track" sub-entity over the real "song" entity for tied scores — fixed with a strong/weak/penalty hint tier; (3) a rate-limit storm was silently recorded as confirmed "no match" instead of "unknown, retry" — added an explicit `lookup_failed` distinction so failures never get written as false negatives. **Then found the hand-typed `composer_map.json` values were STILL contaminating the DB** — 32+ songs had old memory-based composer values sitting untouched because a "no match" result never overwrites existing data. Cleared the entire composer/lyricist/genre/release_year/wikidata_id column set to NULL and re-ran clean. Final honest yield: **8/50 songs (16%) got real Wikidata-confirmed data**; 42/50 confirmed genuine no-match (older ghazals/niche sufi tracks mostly — matches the coverage gap already seen manually with Wikipedia). Re-swept composer weight 0→0.4 against the golden set on this clean data. | **0.121 at every weight (0, 0.05, 0.1, 0.15, 0.25, 0.4) — composer has ZERO effect now** | n/a | 6 | **composer-boost result from the earlier entry is RETRACTED as built on non-automated data; current honest state has no validated rerank signal** | This is the most important finding of this whole thread: the earlier "0.502, best result so far" was entirely dependent on the 40/50 hand-typed composer map, not on anything that will scale. With only 8/50 real automated matches, none of which happen to overlap both an anchor and a candidate in this small golden set, composer's benefit disappears entirely. Not a regression in the code — a correction of a false positive that was about to be reported as a real win. **Composer-as-a-concept isn't necessarily wrong** (the fetch mechanism is verified correct and Wikidata's data is trustworthy where present) — the problem is coverage at this corpus size for this specific song mix. Reinforces that Phase 6 (scale to 500-1000+ mainstream-weighted songs) may need to move up in priority: more songs means both more chances for composer to match AND likely better average Wikidata coverage, since this 49-song pilot skews toward exactly the older/niche repertoire Wikidata covers worst. |
| 2026-09-15 | Tried lowering CLAP's base fusion weight (currently 0.30 in `pipeline/embedder.py`) as a candidate fix for the CLAP-alone regression, using a technique that re-derives each song's true unweighted CLAP embedding from its already-saved fused vector (no new network/audio calls needed) then rebuilds at alternate weights. Swept 0.30/0.20/0.10/0.05/0.02. | 0.30→0.121 (best), 0.20→0.091, 0.10→0.099, 0.05→0.094, 0.02→0.094 | n/a | 6 | **kept at 0.30 — the original default is already the best of everything tested** | Clean negative result: lowering CLAP's weight, the obvious next thing to try given AGENTS.md's own comment that CLAP "over-dominates," does not help — every lower weight tested scored worse. Restored 0.30 via the standard `--from-features` rebuild path afterward. |
| 2026-09-15 | **Raw/final embedding path consistency bug found and fixed in 5 places.** Verifying data integrity after the CLAP-weight sweep above surfaced that `data/embeddings_raw/<id>.npy` had gone stale (still 1381d pre-CLAP-fix data) despite search working correctly off `data/embeddings/`. Root cause: `scripts/recover_clap.py`, `index/build_index.py`'s `rebuild_embeddings_from_features()`, `fit_and_compress_pca()`, and the non-PCA branch of `finalize_embeddings()`, plus `pipeline/processor.py`'s ingestion path, all read and/or wrote the "raw" vector via `row["embedding_path"]` — which is only actually the raw path immediately after first ingestion; once a corpus is finalized once, that DB column points at the FINAL store instead, silently making every subsequent "raw" read/write operate on the wrong file. This is the exact failure mode the Phase 0 fix was supposed to have eliminated, regressing silently through these five separate code paths — including the ingestion path for brand-new songs, which had never actually been saving a raw backup at all. Fixed all of them to always target `EMBEDDINGS_RAW_DIR`/`EMBEDDINGS_DIR` by explicit path construction, never by trusting the DB column for raw access. Re-ran a fully fresh CLAP recovery (all 50, zero failures) to regenerate a genuinely current raw archive, and confirmed raw/final now match exactly. | 0.144 (3rd "fresh" recovery this session — see note) | 0.267 | 6 | fixed, kept | Search correctness was never affected (FAISS always read the correct final path) — this was purely an archival-integrity bug that would have misled any future rebuild attempt reading from `data/embeddings_raw/`, and would have silently kept failing for every new song ingested going forward. Also notable: three separate "fresh" CLAP recoveries of the same 50 songs this session produced 0.121, 0.148, then 0.144 nDCG@5 — real variance, not measurement noise in the eval script, most likely from yt-dlp occasionally matching a different upload of the same song across runs. This is itself a reason to treat small deltas on a 6-anchor golden set with real skepticism; it isn't just the judgment set that's small, the underlying data pipeline has some inherent run-to-run variance too. **0.144 / 0.267 is the current honest baseline — real CLAP, zero active rerank signals (composer retracted, all others already inert).** |
| 2026-09-17 | **Golden-set expansion via Last.fm — solves both "too slow to hand-label" and "reflects one person's taste."** Owner raised both concerns about growing `golden_relevance.json` by hand. Built `pipeline/lastfm_client.py` (`track.getsimilar` — real listener co-occurrence data, not editorial metadata) + `scripts/expand_golden_from_lastfm.py` (fetches similar tracks per song, keeps only ones that are ALSO in our own catalog, grades by Last.fm's own match confidence). Feasibility checked first via manual page fetches before building anything (same discipline as the Wikidata/MusicBrainz check) — confirmed strong coverage even for a classic Jagjit Singh ghazal, the exact repertoire Wikidata struggled with. Ran on all 50 songs: **21 anchors generated** (vs. 6 hand-reviewed), written to a SEPARATE file `data/eval/golden_relevance_lastfm.json` — never silently merged into the owner-reviewed file. Also fixed `evaluate_golden.py`'s closing reminder, which was hardcoded to reference the wrong file/provenance regardless of which golden set was actually loaded — now reads the loaded file's own `_readme`. | **0.231 across 21 anchors** (vs 0.144 across 6 on the hand-reviewed set) | 0.114 | 21 | new eval asset, kept separate from owner's file pending review | Grade thresholds in `_grade_from_match()` are an UNCALIBRATED starting guess (match >= 0.4 -> grade 2, >= 0.1 -> grade 1) — not yet validated against real data beyond a visual spot-check of the first 5-song run, which looked sensible (e.g. Ranjish Hi Sahi <-> Gulon Mein Rang Bhare, both Mehdi Hassan, match=1.0 — independently confirms the same ghazal cluster the hand-reviewed set already found). Precision@5 (0.114) is notably lower than nDCG@5 here despite the higher nDCG — not a contradiction: many auto-generated anchors have very small judged pools (as few as 1 in-catalog neighbor), which mechanically caps Precision@5 (counts all 5 returned slots, most un-judged = 0) without hurting nDCG the same way (which normalizes against the achievable ideal for that pool size). The 0.231 vs 0.144 gap should NOT be read as "the engine is proven better than we thought" — different anchor sets, different judged-pool composition, not a controlled comparison. Real value: a second, independently-sourced, continuously-expandable evaluation asset that will only get more powerful as the catalog scales (Phase 6) — coverage is fundamentally capped by how many Last.fm suggestions overlap with our own catalog, which grows with corpus size. Owner's hand-reviewed file remains the more carefully curated reference; this is a complement, not a replacement. |
| 2026-09-17 | **Phase 5: cut `raga_probability` from the fused vector.** Researched real alternatives first (owner asked specifically): (1) `E2ERaga` — a real pretrained Hindustani model with downloadable weights (`hindustani_raga_model.hdf5`) and a working inference script, but no disclosed accuracy, no disclosed raga list, no license found, Python 3.6.9 pin, third-party Google Drive hosting — genuinely real but unvalidated, not safe to integrate blind; (2) Wikidata has no structured raga property for these songs (confirmed via API test) — the raga name found earlier (2026-09-15, "Raga Bhairavi" for Kun Faya Kun) was in Wikipedia *prose* on a soundtrack album page, not a clean field, so a reliable automated scraper would be real, fragile engineering work, not yet built. Given neither candidate was safe to trust without further validation, cut the existing fake heuristic (cosine similarity vs. 30 hand-typed binary swara templates) from `build_fused_vector()` rather than replace it with another unvalidated signal. `extract_raga_probability()` still runs and is stored in features JSON for reference; just no longer fused into the similarity vector. Raw fused vector: 1893d → 1863d. | 0.144→0.143 (hand-reviewed), 0.231→0.242 (Last.fm) | 0.267→0.267, 0.114→0.124 | 6 / 21 | **kept removed** — no regression either set | Both leads (E2ERaga, Wikipedia raga scraping) documented in `plan.md` Phase 5 as real, promising, but requiring actual validation work (download+test E2ERaga's real accuracy and raga-list overlap; build and test a soundtrack-page scraper) before either is trusted in production — not abandoned, just not rushed. |
